pipeline {
  agent any

  stages {
    stage('build') {
      steps {
        sh 'echo "Building..."'
        sh 'echo "Building again"'
        sh 'echo "Building without ngrok"'
        sh 'echo "Building without ngrok link"'
      }
    }
  }
}